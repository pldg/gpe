{
  "scripts": {
    "build:images": "node ./build/responsive-images.js",
    "build:webpack": "webpack --config ./build/webpack.config.js --mode development",
    "build": "npm run build:images && npm run build:webpack"
  },
  "devDependencies": {
    "clean-webpack-plugin": "^3.0.0",
    "file-loader": "^4.0.0",
    "html-webpack-plugin": "^3.2.0",
    "prims": "^0.2.0",
    "webpack": "^4.35.0",
    "webpack-cli": "^3.3.5"
  },
  "engines": {
    "node": ">= 8"
  }
}
